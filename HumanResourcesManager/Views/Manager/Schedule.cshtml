@model HumanResourcesManager.Models.Manager.ManagerScheduleIndexViewModel
@{
    ViewData["Title"] = "Lịch tăng ca";
    ViewData["PageTitle"] = "Lịch tăng ca";
    ViewData["PageSubtitle"] = "Tạo và quản lý lịch tăng ca";
    Layout = "~/Views/Manager/_ManagerLayout.cshtml";
}

@await Html.PartialAsync("_ManagerHeader")

<div class="p-5 lg:p-8 bg-slate-50/50 min-h-screen">
    @await Html.PartialAsync("_ManagerStats")

    <div class="bg-white rounded-2xl shadow-sm border border-slate-200/60 overflow-hidden">
        @await Html.PartialAsync("_ScheduleTab", Model)
    </div>
</div>

